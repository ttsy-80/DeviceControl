<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/v2_mode_spinner_popup_width"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/ivModePopupCaret"
        android:layout_width="@dimen/v2_popup_caret_width"
        android:layout_height="@dimen/v2_popup_caret_height"
        android:contentDescription="@null"
        android:src="@drawable/ic_v2_popup_caret_trapezoid" />

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardModePopup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="0dp"
        app:cardBackgroundColor="@color/v2_surface"
        app:cardCornerRadius="4dp"
        app:cardElevation="0dp"
        app:cardUseCompatPadding="false">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="24dp"
            android:paddingTop="24dp"
            android:paddingBottom="20dp">

            <TextView
                android:id="@+id/btnModeOptionAuto"
                android:layout_width="match_parent"
                android:layout_height="@dimen/v2_mode_spinner_pill_height"
                android:background="@drawable/bg_v2_mode_spinner_pill_auto"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:text="@string/v2_auto_mode"
                android:textColor="@color/v2_text_primary"
                android:textSize="15sp" />

            <TextView
                android:id="@+id/btnModeOptionManual"
                android:layout_width="match_parent"
                android:layout_height="@dimen/v2_mode_spinner_pill_height"
                android:layout_marginTop="20dp"
                android:background="@drawable/bg_v2_mode_spinner_pill_manual"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:text="@string/v2_manual_mode"
                android:textColor="@color/v2_text_primary"
                android:textSize="15sp" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</LinearLayout>
